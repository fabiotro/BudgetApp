@model List<BudgetModel>
@{
    ViewData["Title"] = "Budgets";
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <h2>Budgets</h2>
    <a asp-action="Create" class="btn btn-primary">
        <span class="me-2">Lagerbudget erstellen</span>
        <i class="bi bi-plus-square"></i>
    </a>
</div>
<hr />

@if (!Model.Any())
{
    <p class="text-muted">Noch keine Budgets vorhanden. <a asp-action="Create">Jetzt erstellen.</a></p>
}
else
{
    <table class="table table-hover">
        <thead>
            <tr>
                <th>Budget</th>
                <th>Zeitraum</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var budget in Model)
            {
                <tr>
                    <td>
                        <strong>@budget.Name</strong>
                        @if (!string.IsNullOrEmpty(budget.Description))
                        {
                            <br /><small class="text-muted">@budget.Description</small>
                        }
                    </td>
                    <td>
                        @($"{budget.StartDate:dd.MM.yyyy} – {budget.EndDate:dd.MM.yyyy}")
                    </td>
                    <td class="text-end">
                        <a asp-action="Detail" asp-route-id="@budget.Id" class="btn btn-sm btn-outline-primary">
                            <i class="bi bi-eye"></i>
                        </a>
                        <a asp-action="Edit" asp-route-id="@budget.Id" class="btn btn-sm btn-outline-secondary">
                            <i class="bi bi-pen"></i>
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
